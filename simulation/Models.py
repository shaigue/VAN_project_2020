"""This is a class that represents the observation Models."""
# might we want something like this?
# model(M, N)
# belief = model.predict()
# model.add_observation(x,y,Z)
# model.train()
# model.eval()?
